window.docContents[86].resolve({"/Ch01TacticMentalModel/#derive-reader":{"contents":"下面加入只读背景。假设一组化简 tactic 都要读取同一张化简规则表。这张表在整段证明中保持不变，却要供 simp、其子调用以及 allGoals 产生的各个调用反复读取。\n\n这份规则表该放哪？\n\n* 放进每个 Goal 会造成重复。规则表与具体目标无关，而局部假设才是每个目标各自携带、会随 intro 扩展的数据。* 放进可写的 State 虽然可行，却允许任意 tactic 修改规则表。原本可由类型保证的只读性质，便只能依靠人工检查。\n\n所以正确的做法是给它单独一格，一个只进不出的只读通道。把机器的形状扩一格：\n\n[伪代码]\n\nMachine A  =  Env → State → ( 失败(Error) | 成功(A, State) )\n                ^^^ 只读背景（如那张化简规则表）：每一步都能拿到，但没有“写回新 Env”的出口\n\n\nEnv 只出现在输入侧，输出只有 (A, State)，没有新 Env。这个类型形状保证计算不能通过该通道修改背景。若子计算需要临时使用另一份背景，应提供带作用域的局部替换操作；子计算结束后恢复原背景。\n\n别让 pure/bind 在扩格后掉队：形状多了 Env 这一格，前面定义的 pure、bind 得跟着抬一层，把 Env 原样透传下去。改动很小：\n\n[伪代码]\n\npure(a)   = fun env => fun state => 成功(a, state)          -- env 读得到，但原样不改\nm >>= k   = fun env => fun state =>\n  match m(env)(state) with                                  -- 把同一个 env 交给 m\n  | 失败(e)         => 失败(e)\n  | 成功(a, state1) => k(a)(env)(state1)                    -- 再把同一个 env 交给 k\n\n\n例如 readRules >>= fun rules => simpWith(rules)：readRules 从 env 取得规则表，不修改 env 或 state；bind 将同一个 env 与前一步返回的 state 交给 simpWith。\n\n于是三种能力，三种待遇，泾渭分明：\n\n* 只读背景（Env）：进得来、改不了。想局部换，用专门的带作用域的操作。* 可变状态（State）：读写都行，一步步接力往下传。* 失败（Error）：随时能抛，抛了就短路冒泡。\n\n这三种效果具有不同接口。将它们都塞进一个大 State 虽然可以运行，却会丢失“只读”“可写”和“失败短路”的类型区分。\n\n","context":"Lean 4 自动化内幕\u0009tactic 心智模型\u0009受够了重复：把共同的形状抽出来","header":"2.5.4. 只读背景：它凭什么不一样","id":"/Ch01TacticMentalModel/#derive-reader"},"/Ch11Aesop/#ch11-config":{"contents":"\n\n\n\n\n\n","context":"Lean 4 自动化内幕\u0009aesop：可配置的启发式搜索\u0009用法详解","header":"13.1.6. 配置 aesop","id":"/Ch11Aesop/#ch11-config"}});