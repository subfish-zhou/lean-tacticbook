window.docContents[127].resolve({"/Ch06Simp/#elab-simp-args":{"contents":"elabSimpArgs 逐个处理方括号内的参数，每个参数可以是多种形式：\n\n-- [Lean 4 v4.30.0-rc1, Lean/Elab/Tactic/Simp.lean:L220-L278]\nprivate def elabSimpArg (indexConfig : Meta.ConfigWithKey) (eraseLocal : Bool)\n    (kind : SimpKind) (arg : Syntax) : TacticM ElabSimpArgResult := withRef arg do\n  try\n    if arg.getKind == ``Lean.Parser.Tactic.simpErase then\n      -- ^^^ `-foo` 语法：从引理集移除\n      let fvar? ← if eraseLocal then Term.isLocalIdent? arg[1] else pure none\n      if let some fvar := fvar? then\n        return .erase (.fvar fvar.fvarId!)\n      else\n        let id := arg[1]\n        if let .ok declName ← observing (realizeGlobalConstNoOverloadWithInfo id) then\n          if (← Simp.isSimproc declName) then\n            return .eraseSimproc declName\n          else\n            return .erase (.decl declName)\n        -- ...\n    else if arg.getKind == ``Lean.Parser.Tactic.simpLemma then\n      -- ^^^ `foo` 或 `← foo` 或 `↓ foo` 语法\n      let post :=\n        if arg[0].isNone then true\n        else arg[0][0].getKind == ``Parser.Tactic.simpPost\n      -- ^^^ `↓` 前缀表示 pre 阶段，默认（无前缀或 `↑`）是 post 阶段\n      let inv  := !arg[1].isNone\n      -- ^^^ `←` 表示反方向\n      let term := arg[2]\n      match (← resolveSimpIdTheorem? term) with\n      | .expr e  =>\n        let name ← mkFreshId\n        elabDeclToUnfoldOrTheorem indexConfig (.stx name arg) e post inv kind\n        -- ^^^ 判断是定义展开还是等式引理\n      | .simproc declName =>\n        return .addSimproc declName post\n        -- ^^^ 用户传入的是一个 simproc\n      | .ext ext₁? ext₂? h =>\n        return .ext ext₁? ext₂? h\n        -- ^^^ 用户传入的是一个 simp 扩展集（如自定义 simp attribute 名）\n      -- ...\n    else if arg.getKind == ``Lean.Parser.Tactic.simpStar then\n      return .star\n      -- ^^^ `*` 语法：添加所有局部假设\n    -- ...\n\n\n参数处理后的结果在主循环中逐一加入 SimpTheorems：\n\n-- [Lean 4 v4.30.0-rc1, Lean/Elab/Tactic/Simp.lean:L334-L361]\n-- (在 elabSimpArgs 内部)\nfor (ref, arg) in args do\n  match arg with\n  | .addEntries entries =>\n    for entry in entries do\n      thms := thms.uneraseSimpEntry entry\n      thms := thms.addSimpEntry entry\n      -- ^^^ 添加引理到 DiscrTree\n  | .addLetToUnfold fvarId =>\n    thms := thms.addLetDeclToUnfold fvarId\n    -- ^^^ let 变量标记为可展开\n  | .addSimproc declName post =>\n    simprocs ← simprocs.add declName post\n  | .erase origin =>\n    if origin matches .fvar _ then\n      thms := thms.eraseCore origin\n    else if ctx.config.autoUnfold then\n      thms := thms.eraseCore origin\n    else\n      thms ← withRef ref <| thms.erase origin\n      -- ^^^ 带检查的移除（确认引理确实存在）\n  | .ext simpExt? simprocExt? _ =>\n    if let some simpExt := simpExt? then\n      thmsArray := thmsArray.push (← simpExt.getTheorems)\n      -- ^^^ 整个扩展集作为新的 SimpTheorems 层\n  -- ...\n\n\n","context":"Lean 4 自动化内幕\u0009simp——化简的瑞士军刀\u0009源码全景：文件结构与调用链","header":"7.2.4. 参数解析：elabSimpArgs 内部","id":"/Ch06Simp/#elab-simp-args"},"/Ch10NormNum/#builtin-plugins":{"contents":"* 插件：evalAdd —— 处理的 head symbol：HAdd.hAdd —— 示例：2 + 3 = 5* 插件：evalMul —— 处理的 head symbol：HMul.hMul —— 示例：6 * 7 = 42* 插件：evalPow —— 处理的 head symbol：HPow.hPow —— 示例：2 ^ 10 = 1024* 插件：evalDiv —— 处理的 head symbol：HDiv.hDiv —— 示例：(10 : ℚ) / 3* 插件：evalMod —— 处理的 head symbol：HMod.hMod —— 示例：17 % 5 = 2* 插件：evalPrime —— 处理的 head symbol：Nat.Prime —— 示例：Nat.Prime 127* 插件：evalGcd —— 处理的 head symbol：Nat.gcd —— 示例：Nat.gcd 12 8 = 4* 插件：evalDvd —— 处理的 head symbol：Dvd.dvd —— 示例：(3 : ℤ) ∣ 12* 插件：evalLe —— 处理的 head symbol：LE.le —— 示例：(2 : ℝ) ≤ 3* 插件：evalLt —— 处理的 head symbol：LT.lt —— 示例：(1 : ℚ) < 2\n\n","context":"Lean 4 自动化内幕\u0009norm_num ：数值计算框架\u0009核心架构：插件注册表","header":"11.2.3. 内置插件清单","id":"/Ch10NormNum/#builtin-plugins"},"/Ch23Methodology/#three-things-to-do-now":{"contents":"如果你读完本章想立刻动手，建议按以下顺序起步：\n\n1. 从 simp only [...] 开始——把手动证明中反复出现的 simp 引理\n   整理到一个引理集（@[simp] 或自定义 simp set），\n   这是零成本的自动化，见效最快。2. 写一个小 macro——把 3 行以上的固定 tactic 序列封装成一行调用，\n   不需要任何 MetaM 知识，5 分钟内可完成。3. 给现有 tactic 添加测试——为你已经在用的 simp / aesop 调用\n   补上 5 个 example，既是文档也是回归保护。\n\n不要一上来就写完整的搜索 tactic 或 elab——\n那是在前三步都做熟之后才需要考虑的事。\n\n下一章展望数学分析领域的自动化前沿——\n当前最具挑战性的形式化领域，也是自动化最有潜力产生影响的方向。\n\n\n","context":"Lean 4 自动化内幕\u0009从证明模式到自动化：方法论\u000923.10 小结","header":"24.10.1. 现在就能做的三件事","id":"/Ch23Methodology/#three-things-to-do-now"},"/Ch24TowardsAnalysis/#bottleneck-estimation-chain":{"contents":"-- [示意] 三角不等式 + 逐段估计\nexample (h1 : ‖f x - f y‖ ≤ ε / 2) (h2 : ‖f y - L‖ ≤ ε / 2) :\n    ‖f x - L‖ ≤ ε := by\n  calc ‖f x - L‖\n      = ‖(f x - f y) + (f y - L)‖ := by ring_nf   -- ❶ 重写\n    _ ≤ ‖f x - f y‖ + ‖f y - L‖ := norm_add_le .. -- ❷ 三角不等式\n    _ ≤ ε / 2 + ε / 2 := add_le_add h1 h2          -- ❸ 逐段估计\n    _ = ε := by ring                                 -- ❹ 化简\n\n\n* 选择中间点：搜索空间无限——哪个中间量能让两段都可估计？* 三角不等式变体：norm_add_le、dist_triangle 等变体众多* ε 预算分配：等分是最简单情况，不等分需要更复杂的约束求解\n\n","context":"Lean 4 自动化内幕\u0009迈向分析自动化\u000924.3 瓶颈分析","header":"25.3.2. 瓶颈 2：估计链","id":"/Ch24TowardsAnalysis/#bottleneck-estimation-chain"}});