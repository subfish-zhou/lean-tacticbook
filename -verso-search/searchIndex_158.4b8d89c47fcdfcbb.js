window.docContents[158].resolve({"/Ch01MetaprogrammingModel/#pure-bind-state-passing":{"contents":"这一节把 pure 和 bind 对应到 Lean 的真实签名，并用可运行示例展开 bind 所传递的状态。\n\n本章所称“副作用”是广义的计算效果（effect），包括失败、读取环境、读写状态与 IO。它不特指修改全局变量，也不表示代码不可推理。\n\nLean 中的 monad 主要提供两个操作：\n\n[示意]\n\npure : α → M α\n(>>=) : M α → (α → M β) → M β\n\n\npure a 把普通值 a 装进 M 计算里，不带额外的效果（就是你的“把值塞进机器”）。ma >>= f 先跑 ma 拿到 a，再把 a 喂给 f 跑下一步（就是你的“接起来”）。Lean 的 do 语法就是这条流水线的糖衣。\n\nmonad laws 在 Lean 中写成：\n\n* 左单位律：pure a >>= f = f a* 右单位律：m >>= pure = m* 结合律：(m >>= f) >>= g = m >>= (fun a => f a >>= g)\n\n这些定律保证插入或消去无效果的 pure，以及重新结合一串 bind，不会改变计算含义。因此，把若干 tactic 步骤抽成辅助函数或重组 do 块时，可以据此保持行为不变。\n\n在 do 的尾位置，return a 等价于当前 monad 的 pure a。在 for、if 或 match 的非尾分支中，return a 会提前结束整个 do 块；返回值仍由当前 monad 的 pure 包装。\n\n要看清 bind 到底传了什么，State monad 是最好的例子。先用一个简化版：\n\n下面四个 \\[可运行\\] 代码块按顺序放在同一个 .lean 文件里：CounterM、tick、twoTicks、twoTicksExpanded、twoTicksDo 是同一段代码的连续片段，后块依赖前块。分别复制会报 unknown identifier。\n\n[可运行]\n\nabbrev CounterM (α : Type) := StateM Nat α\n\n\ndef tick : CounterM Nat := fun oldState =>\n  (oldState, oldState + 1)\n\n\ntick 拿到旧状态 oldState，返回一对：结果是 oldState，新状态是 oldState + 1。连续跑两次 tick：\n\n[可运行]\n\ndef twoTicks : CounterM (Nat × Nat) :=\n  tick >>= fun first =>\n  tick >>= fun second =>\n  pure (first, second)\n\n#eval twoTicks.run 10\n\n\n输出是 ((10, 11), 12)：结果是 (10, 11)，最终状态是 12。下面把同一过程手动展开，不用 >>=：\n\n[可运行]\n\ndef twoTicksExpanded : CounterM (Nat × Nat) := fun oldState =>\n  let (first, stateAfterFirst) := tick oldState\n  let (second, stateAfterSecond) := tick stateAfterFirst\n  let result := (first, second)\n  (result, stateAfterSecond)\n\n#eval twoTicksExpanded.run 10\n\n\n逐行看状态怎么流动：\n\n1. oldState 是整段计算收到的初始状态 10。2. 第一次 tick oldState：结果 10，新状态 stateAfterFirst = 11。3. 第二次调用没用 oldState，用的是 stateAfterFirst，所以结果 11、新状态 stateAfterSecond = 12。4. 结果组合成 (10, 11)。5. 整段计算把结果和最后状态一起返回。\n\nbind 的核心工作就是：把上一步产生的新状态，交给下一步。do 语法把这条管线藏起来了，但没删掉：\n\n[可运行]\n\ndef twoTicksDo : CounterM (Nat × Nat) := do\n  let first ← tick\n  let second ← tick\n  return (first, second)\n\n\ndo 记法隐藏了上述状态传递；let goal ← getMainGoal 运行右侧的 monadic 计算，并把普通结果绑定到 goal。\n\n","context":"Lean 4 自动化内幕\u0009Lean 中的 tactic 基础设施\u0009Monad：把返回值和计算效果放在一起","header":"3.2.2. pure、bind，兑现你上一章造的那两个操作","id":"/Ch01MetaprogrammingModel/#pure-bind-state-passing"},"/Ch12Grind/#failure-higher-order-dependent-types":{"contents":"-- [示意] ✗ E-graph 是一阶数据结构\nexample (h : ∀ f : Nat → Nat, f 0 = 0) : id 0 = 0 := by\n  grind  -- 可能失败：量化变量是函数类型\n  -- 修复：exact h id\n\n\nE-graph 是一阶结构，量化变量本身是函数时 E-matching 可能找不到合适实例。\n\n","context":"Lean 4 自动化内幕\u0009grind：E-matching 与 Congruence Closure\u000912.7 常见失败模式","header":"14.7.5. 失败 5：高阶 / dependent types","id":"/Ch12Grind/#failure-higher-order-dependent-types"}});