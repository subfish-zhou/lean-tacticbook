import VersoManual
import LeanAutoBook.Helpers

open Verso.Genre Manual
open Verso Code External

#doc (Manual) "tactic 心智模型" =>
%%%
file := "Ch01TacticMentalModel"
tag := "ch01-tactic-mental-model"
%%%

> *本章目标*：不看 Lean 的任何真实实现，自己从零把 tactic 这台机器造出来。我们先只用命题、假设、目标、证明这几样东西，动手设计一个又一个 tactic；每加一个例子，就看旧设计具体在哪里坏掉，再逼着自己修。忍到最后，你会发现自己已经亲手造出了一个东西——那个东西的名字叫 monad，但我先不告诉你。
>
> *本章不写 Lean*：这一章正文里不会出现任何真实的 Lean 定义、`import`、或库里的 API 名字。全部用伪代码。下一章再把这里发明的每个零件对到 Lean 的现实实现上。

这本书讲 Lean 的自动化系统。按常规写法，第一章应该先甩出四层 monad 的定义，然后逐层解释。我不这么写。

我的写法是问一个更笨、也更诚实的问题：*如果这套东西还不存在，我手里只有命题和证明，我该怎么把它造出来？* 我手里最初有什么？我从头造一个 tactic 系统，跟 Lean 真实那套的差距在哪里？这个差距，就是我作为读者往后要补的提升空间。

所以整章的骨架是这样的：先划一小块地，规定清楚我手里有哪些原料；然后设计第一个最简单的 tactic；再拿一个它搞不定的新例子，看它在哪儿崩；为了修这个崩，我加一点新机制；如此往复。每一次“加机制”都不是我拍脑袋想加的，而是被上一个例子逼出来的。等这些机制堆到一定程度，它们会自己长成一个统一的形状。到那时我才会揭晓：你造的这个形状，前人给它起过名字。

先说清楚一件事：本章所有代码块都标着 `\[伪代码\]`。它们*不是* Lean，不能编译，也别去 Lean 里搜这些名字。它们只是一种够精确、让你能在任何普通语言里照着实现出来的记法。

# 先划一块地：我手里有什么

%%%
tag := "the-raw-materials"
%%%

造任何东西之前，先盘点原料。证明这件事，剥到最里面，我手里其实只有这么几样东西。

*命题*：一个可以为真或为假的数学陈述。`P ∧ Q`、`P → Q`、`n + 0 = n` 都是命题。我暂时不关心命题内部长什么样，只把它当成一个能比较、能拆开的对象。

*证明*：一个命题为真的凭据。关键点在这里——在我们要造的这套系统里，证明*不是*一句“我证完了”的口头声明，而是一个能被独立检查的*对象*。你给我一个命题 `P` 和一个自称是它证明的东西，我应该有办法核对这东西到底证没证明 `P`。这条从一开始就要立稳，否则后面整台机器会烂掉：关掉一个目标，绝不能只是“把它从待办清单里划掉”，而必须对应“我确实拼出了一个能通过检查的证明对象”。

*目标*：一个“待证任务”。它至少包含一个我要证的命题，还包含我在证它时手头能用的*局部假设*。写下来大概是这个形状：

\[伪代码\]
```
Goal = {
  hypotheses : 一列 (名字, 命题)   -- 局部上下文：我现在能用的假设
  target     : 命题                -- 我要证的命题
}
```

举个具体的。要证 `P → (Q → P)`，我通常不会一口气证它，而是先把两个前提搬进假设，把问题变成“在 `h1 : P`、`h2 : Q` 之下证 `P`”。这时的目标是：

\[伪代码\]
```
Goal = {
  hypotheses = [ (h1, P), (h2, Q) ]
  target     = P
}
```

注意 `hypotheses` 是给证明用的原料库，`target` 是当前要交付的东西。这两样必须分开记：它们的角色不同，一个是我能*反复取用、引用*的，一个是我要*生产*的。

*成功与失败*：一个 tactic 跑下来，要么成功、要么失败，这是它必须能表达的两种结局。“目标不是我以为的形状”“局部假设里没有我要的东西”——这些都得能被干净地说出来，而不是让程序崩溃或者返回一个骗人的结果。这一条先记着，它后面会长成一整套机制。

盘点完了。我手里就这四样：命题、证明（可检查的对象）、目标（假设 + 待证命题）、成功/失败两种结局。整章不会再偷偷引入别的原料——凡是后面出现的状态、环境、失败通道、组合器，都是我用这四样*造*出来的，不是天上掉的。

## 三样东西别搅在一起

%%%
tag := "three-things-apart"
%%%

动手前先把三个层次分清楚，否则后面必乱。

1. *被证的数学对象*：命题和它的证明。这是静态的、数学的东西。`P → (Q → P)` 为真这件事，跟我用什么顺序去证它没关系。

2. *会变的搜索状态*：我证明过程中那份不断变化的“待办”。一开始只有一个目标；`intro` 之后目标的假设变多了、待证命题变小了；`constructor` 之后目标从一个变成两个。这份状态是*可变的、会转移的*。

3. *tactic 程序本身*：一段读当前状态、改状态、同时往外攒证明的程序。`intro`、`exact`、`constructor` 都是这种程序。

初学最容易犯的错，是把第 2 层和第 1 层混起来，以为“证明就是把目标列表变短”。不对。目标列表是搜索状态，是脚手架；证明是最终盖起来的楼。脚手架可以随便搭随便拆，但只有当那个可检查的证明对象真的被拼出来、且通过检查，楼才算盖成。这个区分，本章后面每一步都要用到。

# 造一个搜索状态：先从一个目标试起

%%%
tag := "state-is-a-list"
%%%

一个自然的偷懒念头是：“一个 tactic 就是把一个目标变成另一个目标。”那就先当搜索状态里*只有一个目标*，看它能撑多久——我不提前给它打补丁，等它真撞墙再改。

\[伪代码\]
```
State = {
  goal  : Goal 或 “无”      -- “无” 表示：没有待办目标了
  proof : 一个“带洞的证明”
}
```

为什么状态里除了目标还挂了个 `proof`？因为这里藏着最要命的一半：*证明去哪了？* 如果 tactic 只改 `goal`，那当目标没了，我凭什么说“证完了”？我什么证明对象都没攒出来。所以状态里必须再挂一份*正在被逐步填满的证明骨架*。

“带洞的证明”是这台机器的心脏。把它想成一张填空题：整张纸的结构已经画好了，但有几个方框空着。tactic 干的事，本质上是*挑一个空方框，往里填一点结构*——填进去的结构里可能又开出新的空方框，也可能一个都不开（直接把这个方框补死）。所有方框都补死，这张纸就是一个完整、可检查的证明。

*给每个洞一个名字*。方框不能是匿名的，否则一旦出现两个长得一样的方框，我就说不清“我到底在填哪一个”。所以每个洞都带一个稳定的身份标签，记作 `HoleId`，写成 `?h0`、`?h1` 这样。证明骨架里的洞用它的 `HoleId` 标记；而每个目标都要*记住自己负责填哪个洞*，于是 `Goal` 多一格：

\[伪代码\]
```
Goal = {
  id         : HoleId              -- 我负责填证明骨架里的哪个洞
  hypotheses : 一列 (名字, 命题)   -- 局部上下文：我现在能用的假设
  target     : 命题                -- 我要证的命题
}
```

由此立下一条贯穿全章的*不变量*：证明骨架里*还开着的洞*，和搜索状态里*还没关的目标*，一一对应——每个开着的洞 `?h` 恰好有一个 `id = ?h` 的目标，反之亦然。没有游离在目标之外的洞，也没有不对应任何洞的目标。“关掉一个目标”，就是把它对应的那个洞合法地填掉。

起始状态就长这样：证明骨架是单独一个洞 `?h0`，目标是一个 `id = ?h0` 的 `Goal`。记住这个画面，本章剩下的一切都是它的展开：*目标是“还有哪个空方框、它的假设和待证命题是什么”的索引，证明骨架是“已经填了什么”的实体。* tactic 成功，不等于目标变少；tactic 成功，等于它合法地动了一下这张填空题。

# 发明第一批 tactic

%%%
tag := "inventing-tactics"
%%%

原料和状态都齐了。现在按“越来越难”的顺序，一个一个把 tactic 造出来。每个 tactic 我都走同一套流程：它现在面对的状态长什么样；朴素实现怎么写；在一个小命题上跑一遍看轨迹；它暴露了什么工程痛点；这个痛点逼我加什么。

眼下状态里只有一个目标 `state.goal`，tactic 就作用在它身上。等哪个 tactic 撑不住“只有一个目标”了，我们再当场扩状态——不提前动手。

## exact：直接交一个证明

%%%
tag := "invent-exact"
%%%

最简单的 tactic：我手上已经有一个证明对象 `p`，我想用它关掉当前目标。数学上就是“这个目标我会证，证明就是 `p`”。

*当前状态*：主目标 `g`，它的待证命题是 `g.target`。我带着一个证明对象 `p` 来。

*朴素实现*：

\[伪代码\]
```
exact(p) = fun state =>
  let g = state.goal
  if 检查(p 是 g.target 的合法证明) then
    -- 把带洞证明里、id 为 g.id 的那个洞，用 p 补死
    let newProof = 在 state.proof 中把洞 g.id 替换成 p
    成功， newState = { goal = 无, proof = newProof }   -- 洞填完了，没有待办了
  else
    失败("给的证明和目标对不上")
```

*执行轨迹*：证 `P`，手里假设 `h1 : P`。起始状态 `goal = (id=?h0, target=P, hyps=[h1:P])`，证明骨架 `?h0`。调 `exact(h1)`：检查 `h1` 是不是 `P` 的证明——是。把洞 `?h0` 补成 `h1`，证明骨架变成 `h1`，`goal` 变成“无”。目标没了，证明骨架完整。证完了。

*暴露的痛点*：看那句 `检查(p 是 g.target 的合法证明)`。这个“检查”不是我随口写的注释，它是整台机器的良心。如果我把 `exact` 实现成“不管 `p` 是什么，直接把 `goal` 置成‘无’”，那目标一样会清空，机器一样会欢天喜地报告“证完了”——但我什么都没证。这就是前面反复强调的：*关目标必须等于合法地填掉那个洞，绝不能只是清掉待办*。所以每个能“关掉”目标的 tactic，内部都得压着一次真正的证明检查。这条痛点先记下，它决定了后面失败通道的设计。

## assumption：在假设里翻找

%%%
tag := "invent-assumption"
%%%

`exact` 要我手动报出证明。更聪明一点：让 tactic 自己去局部假设里翻，看有没有哪个假设正好就是目标。

*当前状态*：主目标 `g`，它带着一列假设 `g.hypotheses`。

*朴素实现*：

\[伪代码\]
```
assumption = fun state =>
  let g = state.goal
  for (name, prop) in g.hypotheses do
    if prop 与 g.target 相同 then
      -- 找到了：这个假设本身就是目标的证明
      return exact(名为 name 的那个假设)  应用到 state
  失败("没有假设能匹配目标")
```

*执行轨迹*：目标 `target = Q`，假设 `[ (h1, P), (h2, Q) ]`。循环：`h1 : P`，`P` 不是 `Q`，跳过；`h2 : Q`，命中，用 `h2` 走一遍 `exact`，关掉目标。成功。若假设里根本没有 `Q`，循环走完，落到最后一行，失败。

*暴露的痛点一（成功不等于目标变少，但这里恰好变少了——留个伏笔）*：`assumption` 每次成功都关掉一个目标，看着又印证了“tactic 让目标变少”的错觉。别急，`intro` 和 `constructor` 马上会打破它。

*暴露的痛点二（“相同”是什么意思？）*：`prop 与 g.target 相同` 这句藏了大问题。是逐字符相同吗？那 `Nat` 和 `ℕ`、`n + 0` 和 `n` 就会被判成不同，哪怕它们其实是一回事。真实系统里这个“相同”是一个复杂的判定，而且*可能还会牵动别的状态*——判一次相等，可能顺手改动搜索状态。我现在先当它是朴素的相等，但心里记一笔：*这里将来是要出事的*。（下一章会揭晓这个“判定相同”在 Lean 里到底是什么、为什么它不纯。）

*暴露的痛点三（重复的失败处理）*：注意 `exact` 和 `assumption` 都在末尾手写了一句“失败(...)”。目前才两个 tactic，已经开始重复。等 tactic 有十几个，每个都手写失败分支、每个调用方都手写“如果失败了怎么办”，会很难看。这个痛点先攒着，`orElse` 那节集中还债。

## intro：把前提搬进上下文

%%%
tag := "invent-intro"
%%%

这是第一个真正有意思的 tactic，也是第一个打破“目标变少”错觉的。证 `A → B`，标准动作是：假设 `A`，去证 `B`。目标数没变（还是一个），但目标的*内容*变了，而且——关键——证明骨架这边欠下了一笔债。

*当前状态*：主目标 `g`，`g.target` 形如 `A → B`。

*朴素实现*：

\[伪代码\]
```
intro(name) = fun state =>
  let g = state.goal
  match g.target with
  | (A → B) =>
      let h' = 新建一个 HoleId                          -- 给新洞起个名，比如 ?h1
      let g' = {
        id         = h'                                 -- 新目标负责填新洞
        hypotheses = g.hypotheses ++ [ (name, A) ]      -- 多了一条假设
        target     = B                                  -- 待证命题变小
      }
      -- 关键：把 id 为 g.id 的洞，替换成一个 lambda，
      -- 它接收一个 A 的证明（叫 name），函数体是新洞 ?h'。
      let newProof = 在 state.proof 中把洞 g.id 替换成
                     (fun name : A => ?h')
      成功， newState = { goal = g', proof = newProof }
  | _ => 失败("目标不是箭头类型，intro 无从下手")
```

*执行轨迹*：证 `P → (Q → P)`。起始目标 `id=?h0, target = P → (Q → P)`，假设 `[]`，证明骨架 `?h0`。

- 调 `intro(h1)`：目标匹配 `A=P`、`B=(Q → P)`。新洞 `?h1`，新目标 `id=?h1, target = Q → P`，假设 `[(h1,P)]`。证明骨架：`fun h1 : P => ?h1`。目标数：1 → 1。
- 再调 `intro(h2)`：目标匹配 `A=Q`、`B=P`。新洞 `?h2`，新目标 `id=?h2, target = P`，假设 `[(h1,P),(h2,Q)]`。证明骨架：`fun h1 : P => fun h2 : Q => ?h2`。目标数：1 → 1。
- 现在目标是“在 `h1:P, h2:Q` 下证 `P`”。调 `assumption`，命中 `h1`，洞 `?h2` 被补成 `h1`。目标数：1 → 0。

最终证明骨架：`fun h1 : P => fun h2 : Q => h1`。这是一个完整、可检查的证明对象。

*暴露的痛点一（“目标变少”彻底破产）*：`intro` 连做两次，目标数一直是 1，它根本没让目标变少，可它每一步都在实实在在地推进证明。所以*把 tactic 理解成“缩小目标列表的函数”是错的*。正确的理解是：tactic 在动那张带洞的证明——`intro` 往洞里填了一层 lambda，并开出一个新的、更小的洞。目标列表只是这些洞的索引。

*暴露的痛点二（证明骨架必须被携带和更新）*：看这三步，真正被反复读写的不是别的，正是那个 `state.proof`。`intro` 要往里塞 lambda，`assumption` 要往里补叶子。也就是说，*每个 tactic 都在同一份不断演化的结构上接力施工*。这意味着我的实现里，这份 `state`（尤其是 `proof` 和 `goal`）必须一步步*手动传下去*：上一个 tactic 产出的 `newState`，得原封不动喂给下一个 tactic。眼下才三步就已经开始累赘。这个“状态得手动接力传递”的痛，是本章后半程的主线，先记在这里。

## constructor：一个目标裂成两个

%%%
tag := "invent-constructor"
%%%

现在轮到那个真正撞破“只有一个目标”的 tactic。证 `P ∧ Q`，要分别证 `P` 和 `Q`，再把两个证明*合*成一个 `P ∧ Q` 的证明。

*先看它怎么撞墙*：主目标 `g`，`g.target = A ∧ B`，它对应证明骨架里的洞 `g.id`。合取的证明形如 `And.intro <A的证明> <B的证明>`，所以我要把洞 `g.id` 填成 `And.intro ?hL ?hR`——一下开出*两个*新洞。按不变量，每个开着的洞都得有一个目标认领它，于是我需要*两个*目标：一个 `id=?hL, target=A`，一个 `id=?hR, target=B`。

可我现在的 `State` 只有一格 `goal`，装得下一个目标，装不下两个。*这就是单目标状态第一次真正撑不住的地方*——不是我预先猜到的，是 `constructor` 当场把它顶穿了。`exact`（目标变“无”）和 `intro`（目标还是一个）都没逼出这一步，`constructor` 逼出来了。

*现在才扩状态*，把 `goal` 从“一个或无”升级成“一列”：

\[伪代码\]
```
State = {
  goals : 一列 Goal      -- 待处理目标列表，空列表 = 没有待办
  proof : 带洞证明
}
```

顺手补两个记法：`focus(state)` 取列表里的第一个目标（约定 tactic 默认作用在它身上，叫*主目标*），`rest(state)` 是其余目标。前面写好的 `exact`、`assumption`、`intro` 只需机械地改一处：把 `state.goal` 换成 `focus(state)`，把结果放回列表——`exact` 关掉主目标就返回 `{ goals = rest(state), ... }`，`intro` 把更新后的主目标放回列表头 `{ goals = [g'] ++ rest(state), ... }`。逻辑一个字没变，只是状态多了“还有别的目标排队”这件事。空列表现在就代表原来的“无”。

*朴素实现*：

\[伪代码\]
```
constructor = fun state =>
  let g = focus(state)
  match g.target with
  | (A ∧ B) =>
      let hL = 新建一个 HoleId
      let hR = 新建一个 HoleId
      let gLeft  = { id = hL, hypotheses = g.hypotheses, target = A }
      let gRight = { id = hR, hypotheses = g.hypotheses, target = B }
      -- 关键：把洞 g.id 替换成 And.intro，它的两个参数就是两个新洞
      let newProof = 在 state.proof 中把洞 g.id 替换成
                     (And.intro ?hL ?hR)
      成功， newState = { goals = [gLeft, gRight] ++ rest(state), proof = newProof }
  | _ => 失败("目标不是合取，constructor 用不上")
```

*执行轨迹*：证 `P ∧ P`，手里有 `h : P`。起始 `goals = [ (id=?h0, target=P∧P, hyps=[h:P]) ]`，证明骨架 `?h0`。

- 调 `constructor`：洞 `?h0` 填成 `And.intro ?hL ?hR`，目标裂成 `[ (id=?hL, target=P), (id=?hR, target=P) ]`。目标数：1 → 2。*目标变多了。* 注意这两个 `P` 的待证命题一模一样，但它们的 `id` 一个是 `?hL`、一个是 `?hR`——*靠洞的身份，它们始终分得清*。
- 主目标是 `id=?hL` 的 `P`。调 `assumption`，命中 `h`，洞 `?hL` 补成 `h`。目标数：2 → 1。剩下 `id=?hR` 的 `P`。
- 调 `assumption`，命中 `h`，洞 `?hR` 补成 `h`。目标数：1 → 0。

最终证明骨架：`And.intro h h`。完整、可检查。

*暴露的痛点一（tactic 会让目标变多）*：`constructor` 明晃晃地把 1 个目标变成 2 个。至此，“tactic 总是缩小目标数”这个模型已经被三种方式打破：`assumption` 减少、`intro` 不变、`constructor` 增加。请把它彻底忘掉。

*暴露的痛点二（合并证明 = 洞之间的共享约束）*：`constructor` 开的两个洞*各有身份*（`?hL`、`?hR`），这一点没变；但它们不是两张互不相干的纸——它们是*同一个* `And.intro` 的两个参数。也就是说，玩具里的洞并非“毫无关联的独立任务”：它们通过共享的证明骨架彼此牵连，一个洞被填了什么，会影响整份证明长什么样。*这里要区分两件事*：洞的*身份*一直是清楚、独立的（`?hL` 永远不是 `?hR`）；被简化掉的，是洞之间更强的*依赖/共享约束*——在我们的玩具里，填 `?hL` 不会反过来限制 `?hR` 能填什么。真实系统里这种跨洞的相互牵制会强得多，那正是下一章“元变量”要正面处理的核心难题；本章先用“同一份 proof 骨架里带身份的洞”把它稳稳兜住。

## skip 与 swap：证明不动，只动脚手架

%%%
tag := "invent-skip-swap"
%%%

再造两个“反直觉”的小 tactic，专门用来敲碎残余的错觉。

`skip` 什么都不做，直接成功：

\[伪代码\]
```
skip = fun state => 成功， newState = state
```

它成功了，目标数一点没变，证明骨架一点没动。这说明*“成功”这个结局本身不承诺任何进展*——它只承诺“这一步没出错”。

`swap` 把前两个目标调换顺序：

\[伪代码\]
```
swap = fun state =>
  match state.goals with
  | [g1, g2, ...rest] =>
      成功， newState = { goals = [g2, g1, ...rest], proof = state.proof }
  | _ => 失败("目标不足两个，没法交换")
```

`swap` 成功了，目标数不变，证明骨架不变，它只重排了我接下来处理目标的*顺序*。为什么要有这种东西？因为有时候第二个目标更容易，先干掉它能让第一个目标里的某些洞被顺带确定。顺序是一种策略资源。

*暴露的痛点*：`skip` 和 `swap` 逼出一个精确的结论——*搜索状态的三个部分（目标列表、目标顺序、证明骨架）是可以各自独立变动的*。`exact` 动列表长度和证明；`intro` 动目标内容和证明；`constructor` 动列表长度和证明；`swap` 只动顺序；`skip` 全不动。所以我那个 `State` 结构不能再被含糊对待，它是一份*有内部结构、会以多种方式转移*的状态。“tactic = 状态转移函数”这个形状，到这里已经稳稳立住了。记住这个形状，它待会儿会变成整章的枢纽。

# 让 tactic 组合起来

%%%
tag := "composing-tactics"
%%%

单个 tactic 有了，但真实证明是把它们*串起来*用的：先 `intro`，再 `constructor`，然后每个分支各自 `assumption`。我需要组合器。造组合器的过程，会把前面攒下的所有痛点集中引爆。

## then：先做这个，再做那个

%%%
tag := "invent-then"
%%%

最基本的组合：`t1 then t2`，先跑 `t1`，成功后在它产出的状态上接着跑 `t2`。

*朴素实现*：

\[伪代码\]
```
(t1 then t2) = fun state =>
  match t1(state) with
  | 失败(e)      => 失败(e)              -- t1 没成，整体没成
  | 成功(state1) => t2(state1)          -- 把 t1 的新状态交给 t2
```

*执行轨迹*：证 `P → (P ∧ P)`。用 `intro(h) then constructor then assumption`（先不管 `assumption` 只作用主目标的问题，下一节修）。

- `intro(h)`：目标 `P ∧ P`，假设 `[h:P]`；证明骨架 `fun h:P => ?h1`。
- `constructor`：目标裂成 `[P@?hL, P@?hR]`；证明骨架 `fun h:P => And.intro ?hL ?hR`。
- `assumption`：只关掉主目标那个 `P@?hL`……第二个 `P@?hR` 还开着。

*暴露的痛点一（那句 match 到处都在重复）*：看 `then` 的实现——它做的事，是“跑第一步，*如果*成功就把新状态传给第二步，*如果*失败就把失败原样冒泡”。这个“拆开结果、判断成功失败、成功就把状态往下传、失败就短路”的模式，我在 `assumption`（里面调 `exact`）、在每一个想调用别的 tactic 的地方，都要重写一遍。三个 tactic 的时候还能忍，串起十几步就是灾难：*每一处组合点都在手写同一段拆包-判断-传状态-或短路的样板*。这是本章后半程要集中偿还的一笔账，现在它露出了全貌。

*暴露的痛点二（状态在手里传得越来越绕）*：`state → state1 → state2 → ...` 这条链，每一环我都得亲手接。链一长，我得给每个中间状态起名字，还得保证不把 `state` 手滑写成 `state1`。前面 `intro` 那节点出的“状态得手动接力”，在组合器这里被放大成主要矛盾。

## orElse：不行就换一招

%%%
tag := "invent-orelse"
%%%

第二个组合：`t1 orElse t2`，先试 `t1`，*如果它失败*，就退回原状态、改试 `t2`。这是搜索和回溯的雏形。

*朴素实现*：

\[伪代码\]
```
(t1 orElse t2) = fun state =>
  match t1(state) with
  | 成功(state1) => 成功(state1)
  | 失败(_)      => t2(state)      -- 注意：交给 t2 的是原始 state，不是 t1 弄脏的
```

*执行轨迹*：目标 `Q`，假设 `[h1:P, h2:Q]`。跑 `exact(h1) orElse assumption`。

- `exact(h1)`：`h1:P`，`P` 不是 `Q`，失败。
- 回退，用*原始* `state` 跑 `assumption`：命中 `h2`，成功。

*暴露的痛点（回滚：最容易造错的一步）*：`orElse` 的实现里，我特意写了“交给 `t2` 的是原始 `state`”。为什么要强调？因为 `t1` 在失败*之前*，可能已经改动过状态了——它可能开了新目标、往证明骨架里塞了半截结构、给某个洞填了一半。如果我把 `t1` *弄脏的*状态交给 `t2`，那第一条路的失败就污染了第二条路，`t2` 会在一个残破的现场上工作，结果要么错、要么更难堪的是——它“成功”了，但成功得莫名其妙。

所以 `orElse` 必须*事务性*：`t1` 这一支要么整体成功、要么它的一切改动被完全丢弃，仿佛没跑过。在我这个把状态整个当值传来传去的朴素实现里，回滚是“免费”的——因为我本来就还攥着原始 `state`，`t1` 拿到的是它的副本，弄脏的是副本，我把副本一扔就干净了。

*记住这一点，因为它后面要翻脸*：回滚之所以在这里免费，*完全是因为* 我把状态实现成了“一个到处复制的值”。一旦为了性能，把状态换成“原地修改的可变引用”（真实系统一定会这么干，复制整份大状态太慢），`t1` 的改动就会*就地*生效，`orElse` 就不再能靠“扔掉副本”白嫖回滚了——它得显式地先存档、失败了再回档。*“回滚”这个能力不是任何抽象白送的，它取决于状态和失败是怎么实现、怎么组合的。* 以为“反正包成 monad 了就自动能回滚”，就会写出污染现场的 bug。

## first、repeat、以及作用到哪些目标

%%%
tag := "invent-first-repeat"
%%%

有了 `orElse`，多路选择 `first` 就是把它串起来：

\[伪代码\]
```
first([t1, t2, ..., tn]) = t1 orElse t2 orElse ... orElse tn
```

“重复直到不能再做”是 `repeat`：

\[伪代码\]
```
repeat(t) = fun state =>
  match t(state) with
  | 失败(_)      => 成功(state)        -- 做不动了，就停在当前状态（注意：整体算成功）
  | 成功(state1) => repeat(t)(state1)  -- 做成了，在新状态上接着重复
```

`repeat` 里也藏着回滚：最后一次 `t` 失败时，我要返回的是*那次失败之前*的状态，不是被半途改脏的状态——又一次用到“失败支的改动必须丢弃”。

还有个一直被我拖着的问题：`assumption`、`exact` 这些默认只作用于*主目标*，但 `constructor` 之后我有两个目标，我想让 `assumption` 把它们*都*关掉。于是需要“作用到所有目标”的组合器。这里有个极易踩的坑：*绝不能*把某个目标单拎出来、丢掉其余目标和整份证明骨架去跑 `t`——那会违反“开着的洞和目标一一对应”的不变量。正确的做法是*始终持有完整状态*，只把*焦点*移到某个目标上，并靠每个目标稳定的 `id` 来遍历：

\[伪代码\]
```
allGoals(t) = fun state =>
  -- 先给此刻要处理的每个目标记下稳定 id（快照：处理过程中 goals 会变）
  let todo = [ g.id for g in state.goals ]
  let mutable cur = state              -- 始终持有完整状态：所有目标 + 整份证明骨架
  for hid in todo do
    -- 把焦点移到 id 为 hid 的目标上：只是把它重排到列表最前，
    -- 其余目标、整份 proof 一格都不丢，原样留在 cur 里
    cur = 把 cur 的 goals 重排成“id 为 hid 的目标排在最前”
    match t(cur) with                  -- t 作用在主目标 hid 上，但它看得见完整状态
    | 失败(e)    => return 失败(e)      -- 有一个目标搞不定，整体失败
    | 成功(cur') => cur = cur'          -- t 可能关掉 hid、也可能把它换成若干新目标；完整状态整体接力
  成功， newState = cur
```

注意它和早先那种“把 `[g]` 拼到完整证明骨架上”的错误写法的区别：这里*从没有*把目标从它所属的状态里剥离出来，`t` 每次拿到的都是一份合法的完整状态，只是主目标换了人。遍历靠的是快照下来的稳定 `id`，而不是列表位置——即便处理过程中目标列表被增删重排，我也总能找回“原本该处理的那个洞”。

*执行轨迹*：接上一节 `P → (P ∧ P)` 的现场，`constructor` 之后目标是 `[P@?hL, P@?hR]`，`todo = [?hL, ?hR]`。跑 `allGoals(assumption)`：先把焦点移到 `?hL`，`assumption` 命中 `h`、补死洞 `?hL`；再把焦点移到 `?hR`（靠 id 找到它，哪怕它俩待证命题都是 `P` 也分得清是哪个），`assumption` 命中 `h`、补死洞 `?hR`。最终 `fun h:P => And.intro h h`。完整。

于是整条证明可以写成：`intro(h) then constructor then allGoals(assumption)`。

*暴露的痛点（`allGoals` 把几笔旧账一次性摊开）*：盯着 `allGoals` 的实现看，它同时在做几件事，而且都是我们前面单独遇到过的痛：

1. 它要*读*一份对每个目标都一样的东西（这里还不明显，但真实 tactic 里，环境、选项这类只读信息，得在每次子调用里都能拿到）；
2. 它要*攒*一份贯穿始终的状态（那个 `cur`，尤其是接力的 `proof` 骨架）；
3. 它要*随时可能失败并短路*（任一目标搞不定，整体失败）；
4. 它每一步都在手写“拆开 `t` 的结果 → 判断成功失败 → 成功就把新状态接力下去、失败就短路”这段样板。

第 4 件事，跟 `then` 里那段、`assumption` 调 `exact` 那段，*是同一段样板*——都是“顺着往下走、传状态、失败短路”。我已经把它抄了好几遍。（注意：`orElse` 里那段 `match` *不是*这同一段——它做的是“失败了就换一条路、且回退状态”，那是*选择加回滚*，性质不同，得单独处理，别混进来。）到这一步，顺序组合这条线上的痛点不再是“某个 tactic 难写”，而是“*我所有顺序拼接的地方，骨子里都在重复同一个动作*”。这个被反复手抄的动作，就是下一节要抽出来的东西。

## 一个会返回值的 tactic：先查询，再依据结果行动

%%%
tag := "value-dependent-composition"
%%%

在抽象这段样板之前，得先补一个到现在都被我糊弄过去的场景。前面所有 tactic，产出的“有用信息”都是*状态的变化*，返回值都是“没什么可说”（`Unit`）。但真实证明里，我常常需要一个 tactic 先*查出一个普通值*，然后*下一步的行为取决于这个值*。这种“值依赖”的组合，`then` 那种固定顺序根本表达不了——`then` 只会把返回值丢掉。

举个具体的、我们材料库里就能造的例子。造一个*查询*型 tactic `findHyp(prop)`：它在主目标的假设里找一条类型为 `prop` 的假设，*找到就把那条假设的名字当返回值交出去*，一个证明都不关、状态一格不动：

\[伪代码\]
```
findHyp(prop) = fun state =>
  let g = focus(state)
  for (name, p) in g.hypotheses do
    if p 与 prop 相同 then
      return 成功(返回值 = name, newState = state)   -- 状态不动，只把找到的名字交出去
  失败("没有类型为该命题的假设")
```

它返回的 `name` 是个*普通值*，不是状态变化。现在用它干件真事：目标是 `A ∧ B`，我想直接从假设里翻出一个 `A` 的证明和一个 `B` 的证明，拼成 `And.intro` 交上去。下一步（`exact`）*用得上*前两步查到的名字——这就是值依赖：

\[伪代码\]
```
证明 A ∧ B 的程序 = fun state0 =>
  match findHyp(A)(state0) with
  | 失败(e) => 失败(e)
  | 成功(nameA, state1) =>                 -- 拿到第一个返回值 nameA
      match findHyp(B)(state1) with
      | 失败(e) => 失败(e)
      | 成功(nameB, state2) =>             -- 第二个返回值 nameB
          exact(And.intro nameA nameB)(state2)   -- 关键：这一步真的用到了 nameA、nameB
```

*执行轨迹*：目标 `A ∧ B`，假设 `[ (ha, A), (hb, B) ]`。`findHyp(A)` 返回 `nameA = ha`；`findHyp(B)` 返回 `nameB = hb`；`exact(And.intro ha hb)` 把洞补死。证完。

*暴露的痛点（返回值也得手动接力，而且比状态更烦）*：看这段代码里我亲手拽着两样东西往下传——一样是状态 `state0 → state1 → state2`（老问题），另一样是*返回值* `nameA`、`nameB`（新问题）。`then` 帮不上忙，因为 `then` 设计成*丢掉*返回值。我需要的是一种组合：“跑 `m`，把它的返回值 `a` 拿到手，*用这个 `a`* 决定并跑下一台机器”。这正是逼我把 `Tactic` 的形状从“只吐状态”升级成“*还吐一个普通返回值 `A`*”的那根稻草——没有这个例子，返回值 `A` 就是白加的；有了它，`A` 才付得起自己的租。下一节就把这个形状正式立起来。

# 受够了重复：把共同的形状抽出来

%%%
tag := "extracting-the-shape"
%%%

现在把账一次算清。我在 `exact`、`assumption`、`intro`、`then`、`allGoals`、以及刚才那段值依赖的 `findHyp` 组合里，反复手写的东西到底是什么？我把每个 tactic 的类型摊平来看。

一个 tactic，本质上是这个形状的函数：

\[伪代码\]
```
Tactic = State → 结果
```

而“结果”不是单纯的新状态，它是“失败信息 *或* （某个返回值 × 新状态）”。前面几个 tactic 返回值都是“没什么可返回的”（记作 `Unit`），但刚才 `findHyp` 已经把话挑明了：*一个查询型 tactic 要返回它查到的东西*（那条假设的名字），而且下一步真的会用到这个返回值。所以更诚实的形状是：

\[伪代码\]
```
Tactic 产出 A  =  State → ( 失败(Error)  或  成功(A, State) )
```

读作：一台机器，喂给它一个 `State`，它要么失败给你一个 `Error`，要么成功给你一个普通返回值 `A` *和* 一个新 `State`。把它记成一个类型：

\[伪代码\]
```
Machine A  =  State → Result A
Result  A  =  失败(Error)  |  成功(A, State)
```

*先把三样东西数清楚，它们不是一回事*：这个形状里塞了三种彼此独立的能力，别混成一坨。

- 一份*只读*的背景（前面 `allGoals` 第 1 点提到的、每次子调用都要读的环境/选项）——它进来了但不该被这个通道改，是*读*能力；
- 一份*可读可写*的 `State`（目标列表 + 证明骨架）——是*状态*能力；
- 一条*失败*通道（`Error`）——是*失败*能力。

我先只把“状态 + 失败”显式写进 `Machine A`（背景那份等下单独处理，因为它性质不同）。现在，重看那两个组合器，用 `Machine A` 的眼睛。

## 把普通值塞进机器：pure

%%%
tag := "derive-pure"
%%%

有个操作我一直在偷偷用却没起名。`skip` 想“成功，但什么也不做，也没什么可返回”。更一般地，我经常需要：*手上有个普通值 `a`，我想把它变成一台机器——这台机器不碰状态、不失败，直接成功返回 `a`*。

按 `Machine A` 的定义把它写出来，只有一种写法：

\[伪代码\]
```
把值塞进机器(a) = fun state => 成功(a, state)     -- 状态原样穿过，直接成功返回 a
```

这个操作没得选——`Machine A` 的形状把它唯一确定了。它就是 `skip`（当 `a` 是“无返回值”时）的一般化。给它起个名：`pure`。`pure(a)` = “最平凡的机器：把 `a` 抬进机器世界，其余一律不动”。

## 把两台机器接起来：bind

%%%
tag := "derive-bind"
%%%

真正被反复手抄的是另一个动作。回看 `then`、`allGoals`、还有刚才 `findHyp` 那段值依赖组合里的样板，它们的通用形状是同一个：*我有一台机器 `m`，跑完它拿到一个普通返回值 `a`；我想拿这个 `a` 去决定下一台机器 `k(a)`，然后接着跑*。中间那段“拆开 `m` 的结果 → 失败就短路 → 成功就把返回值喂给 `k`、并把新状态传下去”，我不想再抄了。抽出来：

\[伪代码\]
```
接起来(m, k) = fun state =>
  match m(state) with
  | 失败(e)          => 失败(e)             -- m 失败：短路，k 根本不跑
  | 成功(a, state1)  => k(a)(state1)        -- m 成功：把返回值 a 喂给 k，把新状态 state1 传下去
```

停下来，逐字看这台小机器替我干了什么，正好对上我抄了好几遍的三件事：

- 它*自动传状态*：`m` 产出的 `state1`，被无声地喂给了 `k(a)`。我再也不用手写 `state → state1 → state2` 那条链、再也不用给中间状态起名字。
- 它*自动短路失败*：`m` 一失败，`k` 直接不跑，错误原样冒泡。我再也不用在每个组合点手写 `match ... 失败 => 失败`。
- 它*把返回值接到下一步*：`a` 干净地流进 `k`——这一条正是 `findHyp` 那个例子逼出来的，`then` 给不了。

给它起名：`bind`，记作 `m >>= k`。

先看它怎么把*丢弃返回值*的 `then` 一句话重写：

\[伪代码\]
```
(t1 then t2)  =  t1 >>= (fun _ => t2)      -- t1 的返回值用不上，丢掉，接着跑 t2
```

再看它怎么把那个*用得上返回值*的 `findHyp` 组合收干净——这才是 `bind` 真正的用武之地，`then` 在这里彻底无能为力：

\[伪代码\]
```
证明 A ∧ B 的程序  =
  findHyp(A)  >>= fun nameA =>       -- 拿到返回值 nameA，绑给下一步
  findHyp(B)  >>= fun nameB =>       -- 再拿 nameB
  exact(And.intro nameA nameB)       -- 用上前两步的返回值
```

对比一下这段和前面那坨手写的 `match ... 成功(nameA, state1) => match ...`：状态的接力（`state0→state1→state2`）没了，失败的短路没了，全被 `bind` 吸收进去；`nameA`、`nameB` 却依旧干净地流到最后一步。这就是抽象“付一次租、处处省力”的意思：`bind` 一出现，值依赖的组合从一层层嵌套的 `match` 缩成一串平铺的 `>>=`。

一串纯顺序的 tactic 也从此能顺畅写下去：

\[伪代码\]
```
证明 P → (P ∧ P) 的程序  =
  intro(h)          >>= fun _ =>
  constructor       >>= fun _ =>
  allGoals(assumption)
```

每个 `>>=` 都在替我传状态、传证明骨架、短路失败。我只写“先干这个、再干那个”的骨架，脏活 `bind` 全包了。

*一个必须澄清的点*：`bind` 自动传的是*状态*和*失败短路*，它*不*自动帮你回滚，也*不*自动帮你在失败时改走另一条路。`orElse` 的选择与回滚语义，`bind` 一点忙都帮不上——那得靠下一节单独的接口。`bind` 管的是“顺着往下走”，`orElse` 管的是“出错换路、往回退”，两码事，别指望前者造出后者。

## 第二种接口：选择与回滚（`pure`/`bind` 造不出它）

%%%
tag := "derive-choice"
%%%

`pure` 和 `bind` 收编了*顺序*这条线上的全部样板。但 `orElse`、`first`、`repeat` 属于*另一条线*，它们要的东西 `pure`/`bind` 根本给不了，必须单独立一个接口。

看清差别：`bind` 只在 `m` *成功*时才往下走，`m` 一失败它就直接把失败冒泡出去、`k` 连碰都不碰。可 `orElse` 要的恰恰相反——它要在 `m` *失败*时*抓住*这个失败，*回退*到失败前的状态，再*改走*另一条路。这里有两个 `pure`/`bind` 都不具备的能力：

- *捕获失败*：能拦住本来要冒泡出去的错误，把控制权夺回来（而 `bind` 遇失败只会放行冒泡）；
- *回退状态*：把 `m` 弄脏的状态丢弃、复原到它跑之前（前面 `orElse` 那节反复强调过，这份回滚不是白来的，取决于状态怎么实现）。

所以我得*另外*声明一个接口，专管“尝试—失败就捕获并回滚—换路”，记作 `orElse`（或叫 catch/choice）：

\[伪代码\]
```
(t1 orElse t2) = fun state =>
  存档 state                          -- 回滚要用（值实现里就是攥着原始 state）
  match t1(state) with
  | 成功(a, state1) => 成功(a, state1)
  | 失败(_)         => t2(存档的 state)   -- 捕获失败、回退、改走 t2
```

它*用到* `Machine A` 这个形状，也*配合* `bind`（`t1`、`t2` 内部当然还能用 `>>=` 顺序拼），但它*本身不是* `pure`/`bind` 的组合——`bind` 里没有任何一处能表达“失败时把控制权夺回来”。而 `first` 和 `repeat` 都建立在这个额外接口之上：`first` 是一串 `orElse`，`repeat` 靠“失败了就停在失败前的状态”（又一次捕获加回退）。

*把两条线钉清楚*，这是本章最容易被含糊过去、也最要命的一处分工：

- *顺序 / 值依赖组合* → `pure` + `bind`。这是 monad 的核心两件套。
- *回退 / 选择 / 搜索* → 一个*额外*的 `orElse`/catch 接口 + 一份明确的回滚策略。`repeat`、`first` 依赖它。

一个常见误解是“有了 monad 就自动有了回溯搜索”。不对。monad（`pure`/`bind`）只给你顺序；回溯是你在它之上*另外*加的一层能力，得自己定义、自己保证回滚正确。下一章你会看到真实系统正是把这两件事分成不同的机制来提供的。

## 只读背景：它凭什么不一样

%%%
tag := "derive-reader"
%%%

回头处理我一直搁置的第三样能力：那份*只读背景*。前面一直含糊地提它，现在拿一个具体例子把它*逼*出来，而不是凭空塞进去。

*一个逼出只读背景的例子*：假设我要造一批“化简”类 tactic。它们工作时都要查同一张*化简规则表*——一大堆形如“见到这个形状就改写成那个形状”的规则。这张表在整段证明里是*配置好、固定不动*的：`simp` 要读它，`simp` 调用的子 tactic 要读它，`allGoals` 铺开后每个目标的子调用还要读它。也就是说，*同一份只读数据，被贯穿始终的每一层调用反复读取*。

这份规则表该放哪？

- 放进每个 `Goal`？不行。它跟具体某个目标无关，是*全局*配置；塞进每个目标既冗余，又会在 `constructor` 裂目标时被迫复制来复制去。（对比之下，局部假设 `hypotheses` 就*该*待在 `Goal` 里——它本来就是一个目标一份、会随 `intro` 增长的*局部*数据。别把这两者搞混：假设是每目标私有且可变的，规则表是全局共享且只读的。）
- 放进可写的 `State`？能跑，但会丢一个*保证*。一旦规则表躺在人人可写的 `State` 里，那么任何一步 tactic 都*有能力*顺手改掉它——哪怕我不想。于是当我读一段化简代码、想确认“这几十步里规则表始终是我一开始配的那张”，我没法一眼断定，得逐行排查有没有谁偷偷写过它。*把只读数据放进可写通道，就是把“它不会变”这个本可由类型担保的事实，降级成了需要人肉核对的猜测。*

所以正确的做法是给它*单独一格*，一个*只进不出*的只读通道。把机器的形状扩一格：

\[伪代码\]
```
Machine A  =  Env → State → ( 失败(Error) | 成功(A, State) )
                ^^^ 只读背景（如那张化简规则表）：每一步都能拿到，但没有“写回新 Env”的出口
```

注意 `Env` 只在*输入*侧出现，输出侧只有 `(A, State)`，*没有*新的 `Env`。这个不对称就是“只读”的全部含义——形状本身堵死了改背景的路，于是“背景在这段计算里不变”这件事，由类型直接担保，不用再人肉核对。要在一小段子计算里临时换一份背景（比如临时启用一批额外的化简规则），就提供一个专门的*带作用域*的局部替换操作：它只在子计算里换 `Env`、子计算一结束就恢复，绝不让改动漏到外面。

*别让 `pure`/`bind` 在扩格后掉队*：形状多了 `Env` 这一格，前面定义的 `pure`、`bind` 得跟着*抬*一层，把 `Env` 原样透传下去。改动很小：

\[伪代码\]
```
pure(a)   = fun env => fun state => 成功(a, state)          -- env 读得到，但原样不改
m >>= k   = fun env => fun state =>
  match m(env)(state) with                                  -- 把同一个 env 交给 m
  | 失败(e)         => 失败(e)
  | 成功(a, state1) => k(a)(env)(state1)                    -- 再把同一个 env 交给 k
```

*小轨迹*：`readRules >>= fun rules => simpWith(rules)`。`readRules` 从 `env` 里取出规则表当返回值（`env` 不变、`state` 不变）；`bind` 把*同一个* `env` 继续交给 `simpWith`，同时把 `state` 接力下去。`env` 一路只读地穿过每一层，没有任何出口能改它——这正是我们想要的。

于是三种能力，三种待遇，泾渭分明：

- *只读背景（Env）*：进得来、改不了。想局部换，用专门的带作用域的操作。
- *可变状态（State）*：读写都行，一步步接力往下传。
- *失败（Error）*：随时能抛，抛了就短路冒泡。

这三样是*三种不同的效果*，不是一种效果的三个别名。把它们全塞进一个大 `State` 是能跑，但那样就抹掉了“这个只能读、那个能读写、还有一个是控制流短路”的关键区别——而这些区别，正是你读别人 tactic 代码时最需要的信息。

## 还有第四种：会漏出去的效果

%%%
tag := "derive-io"
%%%

再加一个例子，专门用来说明“回滚状态 ≠ 回滚世界”。假设我想让 tactic 一边跑一边*打印*调试信息（`trace`），或者跟外部工具通信。把这种“打印/对外交互”的能力也加进机器：

\[伪代码\]
```
带 IO 的机器 A  =  Env → State → IO ( 失败(Error) | 成功(A, State) )
                                    ^^ 这一步真的会向外部世界发生作用
```

它和状态有个致命区别。前面说 `orElse` 失败时能靠“扔掉脏状态”回滚——那是因为状态住在我手里，我说扔就扔。但*打印到终端的字*、*写进文件的内容*、*发出去的网络请求*，一旦发生就*收不回来*。就算我把 `State` 完美回滚到打印之前，那行字还留在屏幕上。

*IO 逼 `bind` 再长一项本事*：形状里多了 `IO` 这一层，`pure`/`bind` 又得跟着抬一层——而且这一次不只是“把 `env` 透传”那么轻。`bind` 现在必须*显式地先执行 `m` 那一步的 IO 动作、拿到它的结果、再接着执行 `k` 那一步的 IO*。换句话说，IO 带来了它*自己的一套顺序执行语义*，`bind` 得把它编排进去：

\[伪代码\]
```
m >>= k   = fun env => fun state =>
  先真正执行 m(env)(state) 这一步 IO，得到结果 r     -- 这里会真的向外部世界发生作用
  match r with
  | 失败(e)         => 直接返回 失败(e)（IO 已发生，收不回）
  | 成功(a, state1) => 接着真正执行 k(a)(env)(state1) 那一步 IO
```

*小轨迹*：`trace("开始") >>= fun _ => simpWith(rules)`。`bind` 先让 `trace` 真的把“开始”打到屏幕上（此刻世界已经被改了、撤不回），再接着跑 `simpWith`。要是这之后某个 `orElse` 回滚了 `state`，屏幕上那行“开始”*仍然在*——`bind` 编排的 IO 顺序是“真发生”，不是“可撤销的记账”。

所以：*状态可回滚，IO 不可回滚*。这不是抽象的技术细节，是日常经验——说出去的话泼出去的水，比重开一个证明目标难收多了。这也是为什么真实系统会把“可回滚的证明状态”和“不可回滚的对外输出”当成*不同*的效果分开对待，而不是笼统地叫“副作用”。一个把 IO 和 State 混为一谈的心智模型，迟早会在“我明明回滚了，怎么日志还是双份”这种 bug 上栽跟头。

# 你刚发明的东西叫 monad

%%%
tag := "it-is-a-monad"
%%%

现在可以摊牌了。

你从头到尾都在用一个形状：`Machine A`——一台喂状态、可能失败、能读背景、也许还碰 IO，成功就吐一个 `A` 和新状态的机器。你还从*实际的组合需求*里，逼出了这个形状上的两个操作：

- `pure(a)`：把普通值 `a` 抬进机器，其余不动；
- `m >>= k`（`bind`）：跑 `m` 拿到返回值，喂给 `k` 接着跑，自动传状态、自动短路失败。

*一个类型构造器，配上满足特定规律的 `pure` 和 `bind`，就叫一个 monad。* 就这么回事。你没有“学” monad，你是被 `then`、`findHyp` 组合、`allGoals` 里那段反复手抄的顺序样板逼着*造*出了它。`bind` 不是天降的定义，它是你那段“拆包-判断-传状态-短路”样板的唯一合理归宿。（而 `orElse` 那条选择与回滚的线，是你*另外*加的接口，不在 monad 这两件套里——这个区分别忘。）

Lean 的 `do` 记法，就是 `pure`/`bind` 这条流水线的糖衣。以后你看到

\[伪代码\]
```
do
  let a ← m        -- 跑 m，把普通返回值绑给 a（这一行就是 m >>= fun a => ...）
  let b ← k a      -- 用 a 决定并跑下一台机器
  pure (a, b)      -- 把普通值抬回机器世界
```

就把它读成一串 `>>=`：每个 `←` 底下都藏着一次 `bind`，替你传状态、传证明骨架、短路失败。这跟你手写的那串组合器*一模一样*，只是不用再自己接状态的链了。

## monad 给你什么，不给你什么

%%%
tag := "what-monad-gives"
%%%

抽象有它的成本，也要标清楚它*不*管什么。把话说清楚，免得你把 monad 当万能钥匙。

*monad 给你的*：

- *统一的顺序组合形状*。不管返回值 `A` 是什么，所有机器长一个样、用一套 `pure`/`bind` 接力。`then` 和 `allGoals` 里反复出现的传值、传状态、失败短路因此能被抽掉。`orElse` 和依赖它的 `repeat` 还需要额外的“选择与回滚”接口；它们能和这套机器配合，却不能只凭 `pure`/`bind` 两个操作凭空造出来。
- *自动的状态接力*。`bind` 把上一步的新状态喂给下一步，你不再手写 `state1 → state2` 那条易错的链。
- *在我们造出的这台 `Machine` 里自动短路失败*。这里的失败层是我们自己放进 `Machine` 的，所以它的 `bind` 会让中途错误冒泡、跳过后续步骤；“任何 monad 都自带失败”则是错的，另一个没有失败层的 monad 完全可以没有这项行为。
- *可预测的组合*。这就是所谓 monad laws 的实际含义，别把它当三条天书。它们无非在保证：

\[伪代码\]
```
左单位：pure(a) >>= k        跟直接 k(a)          没区别
右单位：m >>= pure           跟直接 m             没区别
结合律：(m >>= f) >>= g       跟 m >>= (x => f(x) >>= g)  没区别
```

翻成人话：*`pure` 是接线里的空操作，你插进去不改变任何行为；而一串 `bind` 怎么加括号、`do` block 怎么拆分重组，结果都一样。* 你重构 tactic 代码时——把中间几步抽成一个辅助函数、或者把一个大 `do` 拆成两个——之所以敢保证行为不变，靠的就是这三条。它们是“重构安全”的许可证，不是三个要背的公式。

*monad 不给你的*（这几条踩坑率最高）：

- *它不替你回滚*。`bind` 只顺着走。要事务性回滚（`orElse` 那种“失败支的改动必须当没发生”），得靠你怎么*组合*状态与失败、以及显式的回溯组合器。把状态实现成可变引用后，回滚还得手动存档/回档。“包成 monad 了就自动能撤销”是错觉。
- *它不把不同效果抹平成一种*。只读背景、可变状态、失败、IO 是*四种*效果。“monad = 一个装状态的盒子”是把它看小了：状态只是它能承载的效果之一。
- *它不让 IO 变得可回滚*。状态能撤，打印出去的字撤不回来。
- *它不替你分清“预期内的失败”和“真出错了”*。tactic 说“这个目标我搞不定”（`orElse` 该据此换路）跟“证明系统本身崩了/接受了一个假证明”是*完全不同*的两回事。前者是正常搜索的一部分，后者是灾难。monad 的失败通道两种都能塞，但*区分它们是你的责任*，类型和 `bind` 不会替你分。这条到下一章会有真实后果。

# 我们的机器 vs 现实：差距就是提升空间

%%%
tag := "toy-vs-reality"
%%%

回到开篇那句话：*我从头造出来这个东西，与现实那个东西的差距，就是我的提升空间。* 现在盘点这个差距，它就是你读下一章的地图。

*我们这台玩具机器已经抓住的*——这些是真的，下一章会一一确认它们在 Lean 里的对应物：

- 证明状态是*一列*目标，不是一个；空列表才算证完。
- 每个目标 = 局部假设 + 待证命题，两者角色分明。
- tactic 是*状态转移*：成功不承诺目标变少（`intro` 不变、`constructor` 变多、`swap` 只重排、`skip` 全不动）。
- 关目标 = 往那张带洞的证明里合法地填结构，*不是*删列表项。
- 顺序组合靠 `pure`/`bind` 抽掉状态接力和失败冒泡；`orElse`/`repeat` 另外要求选择与回滚语义，`allGoals` 另外要求遍历和聚焦目标。不同组合器各自需要什么，现在已经能分清。
- 效果分四种：只读背景、可变状态、失败、IO——各有各的待遇，尤其 IO 不可回滚。

*我们*故意*没造、下一章才会补上的*——把它们分成两堆看，这个分类本身很重要：

一堆是*纯工程加料*，它们让机器更快更好用，但*不改变*上面这套心智模型：

- 用可变引用存状态（为了不复制大状态而提速）——代价是回滚要手动存档；
- 消息系统、trace、诊断输出；
- 心跳/取消（跑太久就强行中断）；
- 源码位置、报错定位、名字卫生；
- 各种性能缓存。

另一堆*会真正改写心智模型*，它们不是“更快的同一台机器”，而是“更强、也更烧脑的另一台机器”，你现在的玩具直觉在这里会不够用：

- *依赖类型*：类型里可以出现值，“命题”和“证明”的界限比玩具里流动得多；
- *元变量与赋值*：`constructor` 那两个“同一个 `And.intro` 的洞”，现实里由*元变量*承载，它们跨目标共享、会被*赋值*，一个目标里解出的洞可能顺手确定另一个目标里的洞。这里要防一个误解：我玩具里的洞*一直有各自的身份*（`?hL` 从不是 `?hR`），被简化掉的*不是*身份，而是洞之间这种更强的*依赖与共享约束*——玩具里填一个洞不会反过来限制另一个洞能填什么，现实里会；
- *定义等价*：`assumption` 里那句含糊的“命题相同”，现实里是一套专门的判定，而且它*不纯*——判一次可能顺手给某个洞赋值，改了状态；
- *精译*：从有省略、有重载的表面语法，靠*预期类型*补全成完整核心项；
- *语法与宏*：tactic 得先被解析成语法树；
- *实例综合*：自动查找类型类实例；
- *环境更新*：往全局环境里加声明；
- *内核检查*：最后那道独立的、谁也糊弄不过去的关卡——它才是“可检查的证明对象”里“可检查”三个字的真正兑现者。

看清这个分类，你就知道该把学习力气花在哪：第一堆照着查文档就能懂，第二堆才是真正拉开差距、值得反复琢磨的地方。

*下一章要回答的问题*，全是本章亲手挖的坑：

1. 我那个 `Env → State → 结果` 的形状，在 Lean 里真的是这样叠出来的吗？叠了几层，为什么正好是那几层？
2. 只读背景、可变状态、失败，Lean 分别用什么承载？哪个数据在“只读背景”里、哪个在“可变状态”里——记错了会怎样？
3. 那些“同一个构造子的洞”，元变量到底怎么跨目标把它们连起来、怎么赋值？
4. `assumption` 里那句危险的“命题相同”，真实的“定义等价”判定危险在哪、副作用是什么？
5. “成功的 tactic 不必减少目标”这条，Lean 的目标列表管理是不是真这么设计的？
6. 我玩具里靠“扔副本”白嫖的回滚，换成可变引用后，Lean 到底怎么做事务？

带着这六个问题翻页。你不是去背 Lean 的 API，你是去核对*你已经发明过的机器*和*现实的机器*，差在哪几颗螺丝——那几颗螺丝，就是你接下来要拧的。

# 练习与设计题

%%%
tag := "mental-model-exercises"
%%%

下面的题都不是背名词，是让你继续当那个“如果我来造会怎么造”的设计者。

1. *造一个 `exactMany`*。写出伪代码：它接收一列候选证明 `[p1, ..., pn]`，依次尝试用每个去关主目标，第一个成功就停。用 `orElse` 和 `first` 把它拼出来，别从头写循环。然后回答：它靠什么保证第三个候选失败时，不会污染第四个候选的现场？

2. *给“成功不减少目标”再举个例*。除了 `constructor`（变多）、`swap`（重排）、`skip`（不变），自己设计一个 tactic，它成功但目标数*不变*、却实实在在推进了证明骨架。提示：想想“把目标改写成一个等价但更好处理的形式”。

3. *亲手触发回滚 bug*。假设我们把 `State` 从“到处复制的值”改成“一个大家共享、原地修改的可变引用”。逐行推演：在这种实现下，`t1 orElse t2` 那个朴素版（“失败了就用原始 state 跑 t2”）为什么会出错？要修好它，`orElse` 在跑 `t1` 之前和 `t1` 失败之后，各需要多做一步什么？

4. *把 `repeat` 用 `orElse` 和 `pure` 重写*。给出伪代码，并说明为什么“做不动了”这一步必须返回*失败之前*的状态，而不是被半途改脏的状态。

5. *区分两种失败*。设计一段程序，它需要区别对待“`assumption` 没找到匹配假设（正常，应触发 `orElse` 换路）”和“我传给证明检查的对象根本不合法（异常，不该被 `orElse` 悄悄吞掉换路）”。你会怎么在失败通道里携带这个区别？为什么把两者混成一种 `Error` 会让 `orElse` 变得危险？

6. *画出带洞证明的演化*。取命题 `P → (Q → (P ∧ Q))`，用 `intro`、`intro`、`constructor`、`assumption`、`assumption` 一步步证它。每一步都把当时的“带洞证明骨架”完整写出来（洞用 `<洞>` 标记），并标注每个洞对应目标列表里的哪个目标。做完你会对“目标列表只是洞的索引”这句话有肌肉记忆。
